import type { Lesson } from "./course-types";

const lessons: Lesson[] = [
  {
    slug: "08-gradient-direction",
    number: 8,
    title: "梯度怎样指出参数的调整方向",
    question: "一个参数稍微变化时，训练系统怎样判断损失会上升还是下降？",
    duration: "40 分钟",
    opening: [
      "模型算出损失以后，只知道这次预测错了多少，还不知道该改哪个参数、向哪边改。如果一共有几千甚至几亿个参数，逐个向左和向右试一遍会非常慢。",
      "梯度解决的是“局部方向”问题：在当前参数附近，每个参数增加一点会让损失怎样变化。训练系统沿着能让损失下降的方向走一小步，再从新位置重新计算。"
    ],
    sections: [
      {
        title: "梯度描述当前位置附近的变化",
        paragraphs: [
          "先想象模型只有一个参数。参数增加一点，损失也增加，说明继续增大会走坏；参数增加一点，损失反而减少，说明增大是当前可尝试的方向。梯度把这种变化的方向和快慢写成一个数。",
          "梯度为正时，增加参数会让损失上升，所以梯度下降会减小参数；梯度为负时，增加参数会让损失下降，所以更新后参数会变大。这里判断的是当前位置附近，不保证一步就能到达全局最低点。"
        ]
      },
      {
        title: "学习率决定每一步走多远",
        paragraphs: [
          "更新规则可以读成：新参数等于旧参数减去“学习率乘以梯度”。学习率很小，每步稳但可能进展慢；学习率过大，一步可能跨过低点，在两侧来回跳，甚至让损失越来越高。",
          "真实网络有许多参数，梯度会为每个参数给出一个分量。它们合在一起形成同一次更新方向，但训练仍要在更新后重新前向计算；旧位置的梯度不能无限沿用。"
        ]
      }
    ],
    example: {
      title: "一个参数怎样连续走两步",
      intro: "设模型只有参数 w，损失规则是“w 与目标 3 的差再平方”。训练从 w=0 开始，系统已经算出当前位置的梯度为 -6。",
      steps: [
        { label: "计算起点损失", text: "w=0，与 3 相差 -3，平方后的损失是 9。负梯度表示在当前位置增大 w 会让损失下降。" },
        { label: "完成第一次更新", text: "学习率取 0.1，新参数是 0-0.1×(-6)=0.6。此时损失为 (0.6-3)²=5.76，确实低于 9。" },
        { label: "在新位置重算", text: "系统在 w=0.6 重新计算梯度，得到 -4.8。第二次更新后 w=0.6-0.1×(-4.8)=1.08，损失约为 3.69，继续下降。" },
        { label: "检查过大步长", text: "若第一步把学习率改成 1，w 会从 0 跳到 6；它与 3 仍相差 3，损失又是 9。方向虽对，步子太大仍可能没有进步。" }
      ],
      conclusion: "梯度给出当前位置的下坡方向，学习率决定沿这个方向走多远。每走一步都要重新计算损失和梯度，训练才形成闭环。"
    },
    takeaway: "梯度不是正确参数的答案，而是当前位置附近“怎样改会让损失下降”的提示；学习率把提示变成一步实际更新。方向与步长缺一不可，更新后还要重新测量。",
    exercise: {
      prompt: "某参数当前为 4，梯度为 2。分别用学习率 0.1 和 1 写出下一步参数；再说明为什么只看梯度符号，不能判断哪一个学习率更合适。",
      checks: [
        "学习率 0.1 时得到 4-0.1×2=3.8。",
        "学习率 1 时得到 4-1×2=2。",
        "指出两步都沿下降方向，但必须比较更新后的损失，才能判断步长是否合适。"
      ]
    },
    terms: [
      { term: "梯度", definition: "损失在当前位置随各参数发生微小变化的方向和快慢。" },
      { term: "梯度下降", definition: "沿着梯度相反方向更新参数，以尝试降低损失的方法。" },
      { term: "学习率", definition: "一次参数更新沿梯度方向移动的步长系数。" },
      { term: "参数更新", definition: "根据梯度和学习率把旧参数改成新参数的动作。" }
    ]
  },
  {
    slug: "09-backpropagation",
    number: 9,
    title: "误差怎样沿网络反向分配",
    question: "最终输出出错以后，靠近输入的参数怎样知道自己对损失产生了多大影响？",
    duration: "45 分钟",
    opening: [
      "一家工厂的成品尺寸不合格，最后一台机器能直接看到偏差，第一台机器却只知道自己的中间产物。多层网络也有同样的问题：损失出现在输出端，参数却分散在前面的每一层。",
      "反向传播不是把答案倒着塞回网络，而是沿着前向计算走过的路径，逐段计算“这一处变化会怎样影响下一处，最终又会怎样影响损失”。"
    ],
    sections: [
      {
        title: "先保存前向传播中的中间结果",
        paragraphs: [
          "前向传播从输入出发，逐层算出中间表示和最终预测。反向传播需要这些中间结果，因为同一个权重的影响会随当时输入而变化：输入为零时，某条连接即使权重很大，也可能没有传递任何信号。",
          "损失先告诉输出应向上还是向下调整。系统再问输出对上一层改变有多敏感，上一层又对更早一层有多敏感。每一段只处理自己的局部关系。"
        ]
      },
      {
        title: "局部影响沿计算链相乘",
        paragraphs: [
          "如果参数 A 的小变化先让中间值变化 2 倍，而中间值的变化又让输出变化 3 倍，那么参数 A 对输出的总影响是 2×3=6 倍。这种把连续局部影响相乘的关系，就是反向传播使用的链式关系。",
          "反向传播得到每个参数对当前损失的梯度，优化器再用学习率更新参数。两者职责不同：反向传播负责算影响，梯度下降负责决定实际走多大一步。"
        ]
      }
    ],
    example: {
      title: "两层乘法链怎样分配调整责任",
      intro: "一个微型网络只有两步：输入 x=2，先乘参数 w₁=3 得到 h=6；h 再乘参数 w₂=0.5 得到预测 3。目标是 5，当前预测偏低。",
      steps: [
        { label: "保存前向结果", text: "第一步保存 x=2、w₁=3、h=6；第二步保存 h=6、w₂=0.5、预测=3。最终损失由预测 3 与目标 5 的差得到。" },
        { label: "检查后层参数", text: "w₂ 每增加 0.1，输出会增加 h×0.1=0.6。把 w₂ 从 0.5 调到 0.6 后，预测变为 6×0.6=3.6，更靠近目标 5。" },
        { label: "检查前层参数", text: "w₁ 每增加 0.1，h 会增加 x×0.1=0.2；这 0.2 再乘 w₂=0.5，输出只增加 0.1。影响必须经过后面的乘法才能到达输出。" },
        { label: "连接到损失", text: "当前预测低于目标，所以能提高预测的两种调整都有助于降低损失；但影响大小不同。反向传播把“输出怎样影响损失”继续乘回来，得到两个参数各自的梯度。" }
      ],
      conclusion: "靠近输入的参数不会直接看到答案，它对损失的影响由整条后续计算链决定。反向传播把一条长责任链拆成可重复使用的局部计算。"
    },
    takeaway: "信息在前向传播中从输入流向预测，误差责任在反向传播中沿同一条计算路径返回。反向传播计算每个参数对损失的影响，优化器才据此更新参数。",
    exercise: {
      prompt: "把例子中的 w₂ 改成 2，其余不变。计算预测值；再说明 w₁ 增加 0.1 时，输出会增加多少，以及为什么后层参数会改变前层参数的总影响。",
      checks: [
        "先得到 h=2×3=6，预测=6×2=12。",
        "w₁ 增加 0.1 会让 h 增加 0.2，再经 w₂=2 放大，输出增加 0.4。",
        "指出前层的局部变化必须经过后层关系，才能影响最终预测和损失。"
      ]
    },
    terms: [
      { term: "反向传播", definition: "沿前向计算路径反向计算各参数对损失影响的方法。" },
      { term: "链式关系", definition: "连续多步影响可以通过相邻步骤的局部影响相乘得到。" },
      { term: "局部影响", definition: "某一步输入发生小变化时，该步输出随之变化的程度。" },
      { term: "计算图", definition: "记录数值经过哪些运算得到最终结果的连接结构。" }
    ]
  },
  {
    slug: "10-training-curves",
    number: 10,
    title: "训练曲线怎样暴露过拟合",
    question: "训练损失继续下降而验证损失开始上升时，模型发生了什么？",
    duration: "40 分钟",
    opening: [
      "学生反复做同一套练习题，练习成绩不断提高，第一次遇到的新题却开始退步。只看练习成绩，会误以为学习仍在改善；把两条成绩放到时间线上，问题才显现。",
      "神经网络训练也要同时记录训练数据和验证数据上的表现。两条曲线不是装饰，它们回答两个不同问题：模型是否越来越贴合旧数据，以及这种改进能否延伸到没参与更新的数据。"
    ],
    sections: [
      {
        title: "两条曲线必须来自不同职责的数据",
        paragraphs: [
          "训练损失来自用于更新参数的数据，下降说明模型越来越能解释训练样本。验证损失来自不参与参数更新的独立数据，下降才说明当前改动也帮助了未见案例。",
          "如果训练损失和验证损失都高，模型可能尚未学到足够关系；如果训练损失继续下降而验证损失持续上升，模型正在把训练数据的偶然细节当成规律，这就是过拟合信号。"
        ]
      },
      {
        title: "停止点看持续趋势，不看一次抖动",
        paragraphs: [
          "验证损失可能因抽样和小批次而上下波动。实际训练通常不会在第一次上升时立刻停止，而会保留验证表现最好的参数，并等待若干轮确认没有继续改善，这叫早停。",
          "曲线只能告诉我们何时开始失去泛化，不能单独说明原因。数据太少、网络容量过大、训练过久或训练与验证分布不同，都可能产生类似形状，还要结合数据和任务检查。"
        ]
      }
    ],
    example: {
      title: "五轮训练应该停在哪里",
      intro: "某图像模型连续训练 5 轮。训练损失依次为 0.80、0.50、0.30、0.18、0.10；验证损失依次为 0.85、0.58、0.42、0.45、0.60。损失越低越好。",
      steps: [
        { label: "观察前两轮", text: "训练损失从 0.80 降到 0.50，验证损失也从 0.85 降到 0.58。模型对旧数据和未见数据同时改善。" },
        { label: "找到验证最低点", text: "第 3 轮训练损失为 0.30，验证损失为 0.42，是五轮中最低的验证损失，因此先保存第 3 轮参数。" },
        { label: "识别分叉", text: "第 4、5 轮训练损失继续降到 0.18、0.10，验证损失却升到 0.45、0.60。模型更会做训练题，却更不会处理验证样本。" },
        { label: "决定停止", text: "若规则是验证损失连续两轮未改善就停止，第 5 轮结束时触发早停，并恢复第 3 轮保存的参数，而不是保留最后一轮。" }
      ],
      conclusion: "最小训练损失出现在第 5 轮，最值得部署的候选却是第 3 轮。训练目标负责更新参数，验证表现负责选择停止点。"
    },
    takeaway: "训练曲线看模型对旧数据的贴合，验证曲线看这种贴合能否迁移。训练继续变好而验证持续变差，是过拟合信号；早停应保存验证表现最好的参数，并允许短期波动。",
    exercise: {
      prompt: "画出 6 轮训练，其中训练损失持续下降，验证损失在第 4 轮最低、随后两轮上升。标出你会保存的参数和触发早停的时刻，并写明两者为什么可能不是同一轮。",
      checks: [
        "保存点放在验证损失最低的第 4 轮。",
        "若采用两轮耐心值，早停发生在第 6 轮结束后。",
        "说明早停时应恢复第 4 轮参数，而不是使用第 6 轮参数。"
      ]
    },
    terms: [
      { term: "训练曲线", definition: "训练过程中模型在训练数据上的指标随轮次变化的记录。" },
      { term: "验证曲线", definition: "训练过程中模型在不参与更新的数据上的指标变化记录。" },
      { term: "过拟合", definition: "训练数据上继续改善，未见数据上的表现却开始变差。" },
      { term: "早停", definition: "验证表现长期不再改善时停止训练，并恢复最佳参数的做法。" }
    ]
  },
  {
    slug: "11-convolution",
    number: 11,
    title: "卷积网络怎样寻找局部图案",
    question: "同一种边缘出现在图像不同位置时，模型怎样复用同一组规则去寻找它？",
    duration: "45 分钟",
    opening: [
      "一条竖直边缘出现在照片左侧或右侧，仍然是同一种局部图案。若普通全连接层为每个位置分别学习一套权重，它必须在许多位置重复学习相似规则。",
      "卷积把一小组共享权重放到图像各个局部区域上扫描。同一个检测规则反复使用，既利用了图像的空间邻近关系，也减少了需要单独学习的连接。"
    ],
    sections: [
      {
        title: "卷积核每次只看一个局部窗口",
        paragraphs: [
          "卷积核是一小块可学习的权重。它覆盖图像的一个局部窗口，把窗口数字与对应权重相乘后相加，得到一个响应；随后移动到相邻位置，用完全相同的权重再次计算。",
          "所有位置的响应按原有空间顺序排成特征图。响应较强的位置表示当前卷积核寻找的局部图案更明显；训练会调整卷积核，使它逐渐对任务有用的边缘、纹理或局部形状产生响应。"
        ]
      },
      {
        title: "共享权重不等于自动认识所有位置变化",
        paragraphs: [
          "共享权重让同一局部检测器能用于不同位置，但完整网络对平移的稳定性还取决于步幅、填充、汇聚方式和后续层。卷积提供了空间复用的结构倾向，不保证对任何移动都给出完全相同的输出。",
          "多层卷积会扩大每个单元能间接看到的范围：前层找局部边缘，后层把相邻边缘组合成更大形状。层级表示由这种逐层组合产生。"
        ]
      }
    ],
    example: {
      title: "一个卷积核怎样找出竖直边缘",
      intro: "有一张 4×4 黑白数字图，左两列为 0，右两列为 1。使用 2×2 卷积核：左列权重都是 -1，右列权重都是 1。每次向右或向下移动一格。",
      steps: [
        { label: "扫描全黑窗口", text: "左上角窗口是 [[0,0],[0,0]]。对应相乘后总和为 0，说明这里没有从暗到亮的竖直变化。" },
        { label: "扫描边界窗口", text: "跨过第 2、3 列的窗口是 [[0,1],[0,1]]。总和为 0×(-1)+1×1+0×(-1)+1×1=2，产生强响应。" },
        { label: "扫描全亮窗口", text: "右侧窗口是 [[1,1],[1,1]]。两行各自得到 -1+1=0，总和仍为 0；亮度本身不是这个卷积核寻找的对象。" },
        { label: "形成特征图", text: "卷积核在上、下两个跨边界窗口都得到 2，其他位置接近 0。特征图因此保留了“竖直边缘出现在哪里”。" }
      ],
      conclusion: "卷积核不是认识物体的完整答案，它只是一个可复用的局部检测器。多层网络会把许多局部响应继续组合成更适合任务的表示。"
    },
    takeaway: "卷积用同一组小权重扫描不同位置，把局部图案的响应排成特征图。它适合利用图像中的局部性和位置复用，但对位置变化的完整稳定性仍要由整套网络检验。",
    exercise: {
      prompt: "把 4×4 图改成左三列为 0、最右一列为 1。仍用本课卷积核，指出强响应会移动到哪两个窗口，并说明卷积核为什么不需要重新学习。",
      checks: [
        "强响应应出现在跨过第 3、4 列的上、下窗口。",
        "说明窗口数值变了，但使用的卷积核权重完全相同。",
        "区分“检测器可复用”与“整个模型一定对平移不变”。"
      ]
    },
    terms: [
      { term: "卷积", definition: "用同一组局部权重在输入的不同位置重复计算。" },
      { term: "卷积核", definition: "在局部窗口上执行加权计算的一小组共享参数。" },
      { term: "特征图", definition: "卷积核在各位置得到的响应按空间顺序组成的结果。" },
      { term: "局部连接", definition: "一个单元只直接读取输入中相邻的一小块区域。" }
    ]
  },
  {
    slug: "12-recurrent-network",
    number: 12,
    title: "循环网络怎样保留序列中的过去",
    question: "处理一句话或一段时间序列时，当前输出怎样使用前面步骤留下的信息？",
    duration: "45 分钟",
    opening: [
      "看到“请不要关闭”与“请关闭”时，最后一个词相同，前面的“不要”却改变了整体意思。序列任务不能只看当前位置，还要把此前信息带到现在。",
      "循环神经网络按顺序逐步读取输入。每一步都把当前输入与上一步留下的隐藏状态合在一起，生成新的隐藏状态；同一套更新规则在整个序列中重复使用。"
    ],
    sections: [
      {
        title: "隐藏状态是一份不断改写的工作记录",
        paragraphs: [
          "隐藏状态不是把过去原样存进无限记忆，而是把模型认为当前有用的信息压进固定大小的数字表示。新输入到来后，旧状态和新输入共同决定下一状态。",
          "因为更新按顺序发生，同一批元素换一个顺序可能得到不同结果。循环结构因此适合处理文字、语音和时间序列等顺序参与含义的数据。"
        ]
      },
      {
        title: "反复更新会让很早的信息逐渐变弱",
        paragraphs: [
          "如果每一步都只保留上一状态的一部分，早期信号经过许多次乘法后会迅速缩小；反向传播中的梯度也可能随长链条变得很小。这使普通循环网络难以学习很远的依赖。",
          "带门控的循环结构会学习何时保留、写入或丢弃信息，可以缓解长距离记忆问题，但不能保证任何长度的序列都被完整记住。"
        ]
      }
    ],
    example: {
      title: "同样三个数字换顺序为什么结果不同",
      intro: "使用一个故意简化的循环规则：新隐藏状态=0.5×旧隐藏状态+当前输入，初始状态为 0。比较序列 [2,0,3] 与 [3,0,2]。",
      steps: [
        { label: "读取第一个序列", text: "读入 2 后状态为 2；读入 0 后状态为 0.5×2+0=1；读入 3 后状态为 0.5×1+3=3.5。" },
        { label: "读取第二个序列", text: "读入 3 后状态为 3；读入 0 后状态为 1.5；读入 2 后状态为 0.5×1.5+2=2.75。" },
        { label: "比较顺序影响", text: "两组输入都包含 0、2、3，但最终状态分别为 3.5 和 2.75。较晚出现的数字被衰减次数更少，因此影响更大。" },
        { label: "观察长期衰减", text: "若状态 1 后面连续遇到五个 0，它会依次变成 0.5、0.25、0.125、0.0625、0.03125。早期信息很快变弱。" }
      ],
      conclusion: "循环网络用隐藏状态让过去参与当前计算，也正因为状态要被反复改写，早期信息可能在长序列中逐渐消失。"
    },
    takeaway: "循环网络用同一更新规则按顺序改写隐藏状态，使过去能够影响当前；顺序因此会改变结果。固定大小的状态经过多次更新也可能遗忘很早的信息。",
    exercise: {
      prompt: "继续使用“新状态=0.5×旧状态+当前输入”，分别计算 [1,0,0,4] 和 [4,0,0,1] 的最终状态，并解释哪一个末尾数字影响更大。",
      checks: [
        "[1,0,0,4] 的状态依次为 1、0.5、0.25、4.125。",
        "[4,0,0,1] 的状态依次为 4、2、1、1.5。",
        "说明同一数字越晚进入，被后续更新衰减的次数越少。"
      ]
    },
    terms: [
      { term: "序列", definition: "元素的先后顺序会参与含义或结果的数据。" },
      { term: "循环神经网络", definition: "按顺序读取输入并反复更新隐藏状态的网络结构。" },
      { term: "隐藏状态", definition: "序列处理过程中从过去传向下一步的内部表示。" },
      { term: "长期依赖", definition: "当前判断需要使用序列中相隔很远的早期信息。" }
    ]
  },
  {
    slug: "13-attention-transformer",
    number: 13,
    title: "Transformer 的注意力怎样连接远处信息",
    question: "序列中的一个位置怎样按当前需要，直接读取其他位置提供的信息？",
    duration: "45 分钟",
    opening: [
      "读到“毛线球滚到沙发下，小猫追着它”时，“它”需要联系前面的“毛线球”。如果信息只能一步步经过中间词传递，距离越远，路径越长。",
      "注意力让当前位置直接为其他位置分配读取权重。Transformer 以这种机制为核心，同时处理一段序列中的位置关系，不必像循环网络那样按一个方向逐步传递隐藏状态。"
    ],
    sections: [
      {
        title: "查询、键和值分开回答两个问题",
        paragraphs: [
          "当前要更新的位置产生查询，可以理解为“我现在需要什么”；每个可读取位置产生键，用来与查询计算匹配程度；同一位置还产生值，表示匹配后真正提供什么信息。",
          "匹配分数经过归一化后变成总和为 1 的注意力权重。模型按权重对各位置的值求加权和，得到当前位置的新表示。权重决定读多少，值决定读到什么。"
        ]
      },
      {
        title: "注意力是 Transformer 的核心部件，不是全部",
        paragraphs: [
          "注意力本身若不加入位置信息，会把相同元素的不同排列看得过于相似；Transformer 因此还需要位置表示。完整层通常也包含前馈变换、残差连接和归一化等部件。",
          "不同注意力头可以学习不同的匹配方式，但单个权重不能自动当成人类可读的因果解释。它表示模型这一次计算中的读取比例，不证明某个词是现实世界里的原因。"
        ]
      }
    ],
    example: {
      title: "目标词怎样汇总三个位置的信息",
      intro: "句子里当前要更新的是“它”。为便于手算，假设模型只考虑“小猫”“毛线球”“沙发”三个位置，得到的注意力权重分别为 0.1、0.7、0.2，总和为 1。三个位置的某一维值分别为 2、6、1。",
      steps: [
        { label: "解释查询", text: "“它”的查询表示当前需要寻找可指代对象；三个名词的键分别与查询比较，匹配分数归一化后得到 0.1、0.7、0.2。" },
        { label: "读取各位置", text: "按权重读取值：小猫贡献 0.1×2=0.2，毛线球贡献 0.7×6=4.2，沙发贡献 0.2×1=0.2。" },
        { label: "形成新表示", text: "三项相加得到 4.6。这个数只是新表示的一维；真实模型会同时计算许多维度，并通过多组注意力头捕捉不同关系。" },
        { label: "检查边界", text: "最高权重指向毛线球，与句意相符，但不能仅凭这组权重宣布模型“理解了指代”。换一句话、换一层或换一个注意力头，权重都可能改变。" }
      ],
      conclusion: "注意力把远处位置变成可直接读取的候选，再按当前查询分配比例。它缩短了信息连接路径，但仍要依靠训练、位置表示和其他层形成完整模型。"
    },
    takeaway: "注意力用查询与键决定各位置读多少，再对值做加权汇总；Transformer 让位置之间可以直接建立动态联系。注意力权重是计算结果，不应自动当成因果解释。",
    exercise: {
      prompt: "某位置对 A、B、C 的注意力权重为 0.2、0.5、0.3，对应值为 4、2、6。算出加权结果；再把 B 的权重提高到 0.7，并自行调整另外两项使总和仍为 1，比较结果。",
      checks: [
        "原结果为 0.2×4+0.5×2+0.3×6=3.6。",
        "新权重必须都不小于 0，且三项总和为 1。",
        "说明结果变化同时取决于权重怎样分配和各位置提供的值。"
      ]
    },
    terms: [
      { term: "注意力", definition: "让一个位置按匹配程度加权读取其他位置值的机制。" },
      { term: "查询", definition: "当前读取位置用来表达“此刻需要什么”的表示。" },
      { term: "键", definition: "每个候选位置用于和查询计算匹配程度的表示。" },
      { term: "值", definition: "候选位置在获得注意力权重后实际提供给汇总结果的信息。" }
    ]
  },
  {
    slug: "14-transfer-learning",
    number: 14,
    title: "什么时候复用已有模型，什么时候从头训练",
    question: "数据和算力有限时，怎样判断预训练模型能否成为新任务的合适起点？",
    duration: "45 分钟",
    opening: [
      "一家小团队只有 800 张商品照片，却想训练瑕疵识别模型。从随机参数开始，模型既要学会边缘和纹理，又要学会哪些纹理代表瑕疵，数据可能远远不够。",
      "预训练模型已经在其他大量数据上学过一套表示。迁移学习尝试复用其中仍然有用的部分，再用新任务数据调整输出或部分参数；关键不是“预训练一定更强”，而是旧表示和新任务是否匹配。"
    ],
    sections: [
      {
        title: "先比较数据形态与任务关系",
        paragraphs: [
          "如果旧模型和新任务都处理自然图像，前层学到的边缘、颜色和纹理常有复用价值；若输入变成完全不同的传感器信号，旧表示可能帮助很少，甚至限制学习。",
          "新数据越少，越需要限制可调整参数，避免模型记住少量样本。常见做法是冻结大部分预训练参数，只训练新的输出层；数据更多或任务差异较大时，再逐步解冻并微调。"
        ]
      },
      {
        title: "选择训练起点仍要用验证数据检验",
        paragraphs: [
          "冻结参数不是永远不改，而是在当前训练方案中暂时保持不变。微调则用较小步长继续调整一部分或全部预训练参数，既保留已有表示，又适应新任务。",
          "预训练来源、数据许可、偏差和输入处理方式都要检查。最可靠的选择不是凭模型名决定，而是在同一验证集上比较简单基线、冻结方案与微调方案；旧经验若让新任务更差，就是负迁移。"
        ]
      }
    ],
    example: {
      title: "三个项目怎样选择训练起点",
      intro: "团队有同一个预训练图像模型，但面对三个新项目：800 张商品瑕疵照片、50,000 张医学影像，以及 20,000 条机器振动的一维传感器序列。",
      steps: [
        { label: "小型商品图像任务", text: "输入仍是常见照片，数据只有 800 张。先冻结图像模型主体，只训练新的瑕疵输出层；再视验证结果解冻靠后的少数层。" },
        { label: "较大医学影像任务", text: "输入仍有空间局部结构，但内容与自然图像差异明显，且有 50,000 张。可以从预训练参数开始，逐步微调更多层，并与从头训练基线比较。" },
        { label: "振动序列任务", text: "数据是一维时间序列，与图像模型预期的通道和空间结构不匹配。优先寻找同类序列预训练模型；若没有，设计合适结构从头训练比强行套用图像模型更合理。" },
        { label: "统一验证口径", text: "三个项目都保留独立验证集，并记录同一指标。迁移方案只有在相同数据切分和评估条件下优于简单基线，才算真正节省了学习成本。" }
      ],
      conclusion: "是否迁移由输入结构、任务相似度、数据量和验证结果共同决定。预训练提供起点，不提供免检的答案。"
    },
    takeaway: "迁移学习复用的是已有表示，不是照搬旧任务答案。数据少且任务相近时先冻结再逐步微调；结构差异大时考虑同类预训练或从头训练，最终都要用独立验证结果裁决。",
    exercise: {
      prompt: "为“2000 张宠物照片分类”“30 万段客服文本分类”“500 条新型工业传感器记录”分别选择冻结、微调、寻找同类预训练或从头训练，并写出一项会改变你选择的新信息。",
      checks: [
        "每项都先比较输入形态和旧模型训练领域。",
        "把数据量与允许调整的参数范围联系起来。",
        "每个选择都保留同口径验证，不把预训练模型当成必然更好的答案。"
      ]
    },
    terms: [
      { term: "预训练模型", definition: "已经在其他数据和任务上学习过参数的模型。" },
      { term: "迁移学习", definition: "把旧任务中学到的表示用于新任务的训练方式。" },
      { term: "微调", definition: "用新任务数据继续小幅调整部分或全部预训练参数。" },
      { term: "冻结参数", definition: "训练新任务时暂时保持一部分已有参数不变。" }
    ]
  }
];

export default lessons;
