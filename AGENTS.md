# 知识解压项目契约

本文件保存长期有效的工作规则。当前进度、下一批任务和交接记录放在 [content/CONTINUATION.md](content/CONTINUATION.md)，不要把临时状态写进本文件。

## 开始工作前

先检查工作区，保留已有改动，不要重做已经完成的批次。然后按下面的顺序阅读权威文件（canonical files）：

1. 本文件：稳定规则和完成标准。
2. [content/CONTINUATION.md](content/CONTINUATION.md)：当前状态、下一步和上次验证结果。
3. [content/overall.md](content/overall.md)：项目为什么采用“压缩 → 解压”。
4. [content/curriculum/README.md](content/curriculum/README.md)：课程总地图和共同结构。
5. 当前课程的 `content/curriculum/...` 大纲：课数、问题、关键概念、完成标志和课程边界。
6. `lib/course-types.ts`、已经完成的同类课程和对应页面：代码结构与呈现方式。

`lib/outline-catalog.generated.ts` 由脚本生成，不得手工修改。如果文件之间出现冲突，以 Dean 在当前任务中的明确要求为最高优先级；不要静默猜测，应在本次改动中修正相关权威文件，或把未决点写进 `content/CONTINUATION.md`。

## 课程方法：先压缩，再解压

“压缩”不是口号，也不是把大纲缩短。它要求先通读全课材料，从中找出：

- 公理句：少量能够解释全课主要现象的句子；
- 最小模型：解决核心问题所必需的对象、变量及其关系；
- 可迁移规律：换一个案例后仍能用于判断、解释或行动的规律。

开始扩写一门新课前，必须使用 `anthropic-skills:dean-distill` 完成这一步，并通过两项检查：

- 归格测试：每一课都能回到最小模型；放不进去的内容必须被说明为边界或例外，否则先修改模型。
- 生成器测试：只凭这套模型，能够解释一个原材料没有直接给出的新现象，或生成一个合理的新案例与判断路径。

两项检查没有通过，不进入正式课程写作。通过后，使用 `dean-writing-standards` 写课，并按信息依赖顺序展开。

## 单课怎样解压

默认读者没有专业基础。每一课按下面的理解顺序推进，顺序是逻辑要求，不必机械做成六个标题：

```text
可观察现象 → 对象 → 机制 → 案例 → 迁移 → 边界
```

- 从读者能看见、听见、操作或核对的现象进入，不先堆术语。
- 用 1–2 句给出清楚、可检验的强断言，随后解释为什么成立。
- 新术语在第一次使用处用短句解释；不要把定义集中到前面让读者记忆。
- 案例必须真的跑过机制，数字、方向和步骤能够复核。
- 迁移部分要换条件，让读者使用模型作出新判断，不能只复述定义。
- 边界要说明规律何时不适用、还缺什么信息，或哪些内容留到进阶课程。

每一课都要有可观察的完成标志，例如“能画出……”“能把六个案例分成……并说明依据”“能按记录复算……”。“理解了”“熟悉了”不是完成标志。

课程大纲只说明要回答的问题、关键概念与完成标志。正式正文还必须提供机制解释、可复核案例、迁移任务和边界。不得把大纲换一种说法、拉长句子后伪装成正文。

## 进度与验证纪律

- 分批写作；每批完成后，在同一次改动中更新 `content/CONTINUATION.md`。
- 只记录实际完成并检查过的内容。部分完成的课程仍算“大纲”，不能提前计入完整教程。
- 交接记录写清本批章节、改动文件、已运行的验证、仍未完成的事项，以及下一次应从哪个具体动作开始。
- 课程全部写完后再接入正式路由、更新课程状态和全站数字。生成目录使用 `npm run catalog:generate`，不要直接改生成文件。
- 内容或代码接入完成后，至少运行 `npm run lint` 和 `npm test`。失败时记录原始失败点，不要把未验证写成已完成。
- 修改课程数量、章节数量、链接、文件名或路由后，逐项核对 README、课程地图、页面文案、测试和 `content/CONTINUATION.md`。

